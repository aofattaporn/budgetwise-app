import 'package:flutter/material.dart';

class MainScreenPlanTab extends StatelessWidget {
  const MainScreenPlanTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PlanScreen"));
  }
}
