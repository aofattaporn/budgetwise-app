import 'package:flutter/material.dart';

class MainScreenTransactionTab extends StatelessWidget {
  const MainScreenTransactionTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("MainScreenTransactionTab"));
  }
}
