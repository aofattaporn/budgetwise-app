import 'package:flutter/material.dart';

class MainScreenAccountTab extends StatelessWidget {
  const MainScreenAccountTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("MainScreenTransactionTab"));
  }
}
