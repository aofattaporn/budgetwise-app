import 'package:flutter/rendering.dart';

class AppRadius {
  AppRadius._();

  /// Radius of 0.
  static const none = Radius.zero;

  /// Extra extra small radius of 2.
  static const xxs = Radius.circular(2);

  /// Extra small radius of 4.
  static const xs = Radius.circular(4);

  /// Small radius of 6.
  static const sm = Radius.circular(6);

  /// Medium radius of 8.
  static const md = Radius.circular(8);

  /// Large radius of 12.
  static const lg = Radius.circular(12);

  /// Extra large radius of 16.
  static const xl = Radius.circular(16);

  /// Extra extra large radius of 24.
  static const xxl = Radius.circular(24);

  /// Maximum rounded corners radius of 32.
  static const max = Radius.circular(32);
}
